# Copy to .env or set as a system environment variable before running the app.
# Atlas: Cluster -> Connect -> Drivers -> copy the connection string and replace <password>.
# Replace YOUR_PASSWORD and YOUR_CLUSTER (from Atlas Connect -> Drivers)
MONGODB_URI=mongodb+srv://mrruchirangarenuka_db_user:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/vehicle-service?retryWrites=true&w=majority
